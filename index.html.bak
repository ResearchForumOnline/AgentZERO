<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>OpenZero // Sovereign CPU AI Claw bot moltbot Command Node</title>

    <meta property="og:title" content="OpenZero // Sovereign AI Node">
    <meta property="og:description" content="Deploy your own Distributed Intelligence Lattice. 10GB Local Payload. No external APIs. Real AI Sovereignty. Architect: Shaf Brady.">
    <meta property="og:image" content="https://openzero.talktoai.org/zero_og_image.png">
    <meta property="og:url" content="https://openzero.talktoai.org">
    <meta property="og:type" content="website">
    <meta name="twitter:card" content="summary_large_image">
    <link rel="icon" type="image/png" href="/zero_og_image.png">

    <style>
        :root { --neon: #00ff41; --bg: #000000; --panel: #0a0a0a; --text: #ffffff; --gold: #d4af37; --dim: #333; }
        body { background: var(--bg); color: var(--text); font-family: 'Courier New', monospace; margin: 0; padding: 0; text-transform: uppercase; overflow-x: hidden; }
        .container { max-width: 1200px; margin: 0 auto; padding: 60px 20px; }

        /* HEADER & TITLES */
        header { border-left: 15px solid var(--neon); padding-left: 30px; margin-bottom: 80px; position: relative; }
        h1 { font-size: 6em; margin: 0; letter-spacing: -8px; line-height: 0.8; }
        .tagline { letter-spacing: 5px; color: #666; font-weight: bold; margin-top: 15px; font-size: 1.2em; }
        .node-url { color: var(--neon); font-weight: bold; margin-top: 30px; letter-spacing: 2px; border: 1px solid var(--neon); display: inline-block; padding: 10px 20px; background: rgba(0, 255, 65, 0.05); }

        /* SECTIONS */
        .section-header { display: flex; align-items: center; margin-bottom: 30px; border-bottom: 1px solid var(--dim); padding-bottom: 10px; margin-top: 80px; }
        .section-num { color: var(--neon); font-size: 1.5em; font-weight: bold; margin-right: 15px; }
        .section-title { font-size: 1.5em; letter-spacing: 2px; color: #fff; }

        /* DEPLOYMENT TERMINAL (ANIMATED) */
        .terminal-window { background: #050505; border: 1px solid #444; padding: 40px; border-radius: 4px; box-shadow: 0 0 30px rgba(0,0,0,0.8); }
        .terminal-header { color: #555; font-size: 0.8em; margin-bottom: 20px; border-bottom: 1px dashed #222; padding-bottom: 10px; display: flex; justify-content: space-between; }
        .typewriter { font-size: 1.4em; color: var(--neon); font-weight: bold; min-height: 1.5em; display: block; background: #000; padding: 20px; border-left: 5px solid var(--neon); word-break: break-all; }
        .cursor { animation: blink 1s infinite; }
        @keyframes blink { 0% { opacity: 0; } 50% { opacity: 1; } 100% { opacity: 0; } }

        /* THE STACK GRID (NEW) */
        .stack-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(250px, 1fr)); gap: 20px; margin-bottom: 60px; }
        .stack-card { border: 1px solid var(--dim); padding: 25px; background: #080808; transition: 0.3s; }
        .stack-card:hover { border-color: var(--gold); transform: translateY(-5px); }
        .stack-icon { font-size: 2em; margin-bottom: 15px; color: var(--gold); }
        .stack-name { font-size: 1.2em; font-weight: bold; color: #fff; margin-bottom: 5px; }
        .stack-desc { font-size: 0.8em; color: #888; }

        /* SOVEREIGNTY MATRIX (NEW) */
        .matrix-table { width: 100%; border-collapse: collapse; margin-top: 20px; border: 1px solid var(--dim); }
        .matrix-table th { text-align: left; padding: 20px; border-bottom: 2px solid var(--neon); color: var(--neon); background: #0a0a0a; }
        .matrix-table td { padding: 20px; border-bottom: 1px solid var(--dim); color: #ccc; }
        .matrix-check { color: var(--neon); font-weight: bold; }
        .matrix-cross { color: #ff0040; font-weight: bold; }

        /* COMMAND GRID (ENHANCED) */
        .cmd-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(500px, 1fr)); gap: 30px; }
        .cmd-card { background: #080808; border: 1px solid var(--dim); padding: 35px; position: relative; transition: 0.2s; }
        .cmd-card:hover { border-color: var(--neon); background: #0a0a0a; }
        .cmd-card h3 { color: var(--gold); margin-top: 0; font-size: 1.4em; border-bottom: 1px solid #222; padding-bottom: 15px; margin-bottom: 20px; }
        .example-block { background: #000; padding: 20px; border: 1px dashed #333; margin-top: 20px; }
        .example-cmd { color: var(--neon); font-weight: bold; display: block; margin-bottom: 8px; font-size: 1.1em; }
        .example-desc { color: #666; font-size: 0.8em; }

        /* NAV & FOOTER */
        .nav-footer { display: grid; grid-template-columns: 1fr 1fr; gap: 20px; margin-top: 80px; }
        .nav-btn { display: block; text-align: center; border: 2px solid var(--gold); color: var(--gold); padding: 30px; text-decoration: none; font-weight: bold; font-size: 1.2em; transition: 0.3s; }
        .nav-btn:hover { background: var(--gold); color: #000; }
        
        .brand-mark { text-align: center; margin: 100px 0 50px 0; }
        .brand-mark img { border: 4px solid var(--neon); padding: 15px; background: #000; box-shadow: 0 0 30px rgba(0, 255, 65, 0.15); }
        footer { border-top: 1px solid var(--dim); padding-top: 40px; color: #444; font-size: 0.8em; text-align: center; letter-spacing: 1px; }

        /* MOBILE OPTIMIZATION */
        @media (max-width: 768px) {
            h1 { font-size: 3.5em; letter-spacing: -4px; }
            .stack-grid, .cmd-grid, .nav-footer { grid-template-columns: 1fr; }
            header { border-left-width: 8px; padding-left: 15px; }
            .typewriter { font-size: 0.9em; padding: 15px; }
        }
    </style>
</head>
<body>

<div class="container">
    <header>
        <h1>OPEN<span style="color:var(--neon)">ZERO</span></h1>
        <div class="tagline">DISTRIBUTED INTELLIGENCE LATTICE // 1111</div>
        <div class="node-url">NODE ACCESS: https://openzero.talktoai.org</div>
    </header>

    <div class="section-header">
        <span class="section-num">00</span>
        <span class="section-title">SYSTEM_ARCHITECTURE_10GB</span>
    </div>
    <div class="stack-grid">
        <div class="stack-card">
            <div class="stack-icon">🧠</div>
            <div class="stack-name">BRAIN: GEMMA 2</div>
            <div class="stack-desc">9B Parameter Local Weights. Google DeepMind Architecture. Offline Inference.</div>
        </div>
        <div class="stack-card">
            <div class="stack-icon">💾</div>
            <div class="stack-name">MEMORY: CHROMADB</div>
            <div class="stack-desc">Vector Embeddings RAG. Stores Research Papers & Lattice Logic.</div>
        </div>
        <div class="stack-card">
            <div class="stack-icon">👁️</div>
            <div class="stack-name">VISION: MOLTBOT</div>
            <div class="stack-desc">Headless Chromium Hand. Navigates, Screenshots, and Scrapes the Web.</div>
        </div>
        <div class="stack-card">
            <div class="stack-icon">⚡</div>
            <div class="stack-name">CORE: FLASK IO</div>
            <div class="stack-desc">Sovereign Interface. Terminal Control. Real-time Websocket Telemetry.</div>
        </div>
    </div>

    <div class="section-header">
        <span class="section-num">01</span>
        <span class="section-title">INITIATE_DEPLOYMENT_SEQUENCE</span>
    </div>
    <div class="terminal-window">
        <div class="terminal-header">
            <span>BASH TERMINAL</span>
            <span>ROOT ACCESS REQUIRED</span>
        </div>
        <p style="color:#888; margin-bottom:10px;">Run this command on any Ubuntu/Debian/Alma/RedHat server:</p>
        <div class="typewriter" id="typewriter"></div>
        <p class="cmd-note" style="margin-top:20px;">
            Payload: 10GB (Post-Install) | RAM Required: 8GB+ | <span style="color:var(--neon)">NO API KEYS REQUIRED</span>
        </p>
    </div>

    <div class="section-header">
        <span class="section-num">02</span>
        <span class="section-title">OPERATIONAL_COMMANDS</span>
    </div>
    <div class="cmd-grid">
        <div class="cmd-card">
            <h3>TERMINAL_SOVEREIGNTY</h3>
            <p>Direct OS control. The agent acts as a chaotic-good SysAdmin.</p>
            <div class="example-block">
                <span class="example-cmd">run ls -la /home</span>
                <span class="example-desc">Lists all system user directories.</span>
            </div>
            <div class="example-block">
                <span class="example-cmd">run nano mission_log.txt</span>
                <span class="example-desc">Edit/Create files via background terminal.</span>
            </div>
        </div>

        <div class="cmd-card">
            <h3>REMOTE_INFRASTRUCTURE</h3>
            <p>Manage secondary nodes via SSH tunneling.</p>
            <div class="example-block">
                <span class="example-cmd">ssh 192.168.1.50 root uptime</span>
                <span class="example-desc">Checks health of a remote server.</span>
            </div>
            <div class="example-block">
                <span class="example-cmd">ssh 10.0.0.5 zero "ls -la"</span>
                <span class="example-desc">Inspect configuration on secondary node.</span>
            </div>
        </div>

        <div class="cmd-card">
            <h3>MOLTBOT_VISION_SYSTEM</h3>
            <p>Autonomous web research via Headless Browser Hand.</p>
            <div class="example-block">
                <span class="example-cmd">go to https://talktoai.org</span>
                <span class="example-desc">Navigates and streams visual state.</span>
            </div>
            <div class="example-block">
                <span class="example-cmd">go to https://google.com</span>
                <span class="example-desc">Perform search query and retrieve visual data.</span>
            </div>
        </div>

        <div class="cmd-card">
            <h3>KNOWLEDGE_BASE_RAG</h3>
            <p>Query the internal Lattice Maximus mathematical archive.</p>
            <div class="example-block">
                <span class="example-cmd">explain the P(G) safety logic</span>
                <span class="example-desc">Brain pulls Feb 2026 paper on ethics.</span>
            </div>
        </div>
    </div>

    <div class="section-header">
        <span class="section-num">03</span>
        <span class="section-title">SOVEREIGNTY_MATRIX</span>
    </div>
    <table class="matrix-table">
        <thead>
            <tr>
                <th>FEATURE</th>
                <th>OPENZERO (YOU)</th>
                <th>CLOUD AI (THEM)</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td>PRIVACY</td>
                <td class="matrix-check">100% LOCAL / AIR-GAPPED</td>
                <td class="matrix-cross">DATA HARVESTED</td>
            </tr>
            <tr>
                <td>COST</td>
                <td class="matrix-check">-bash.00 (OWNDED HARDWARE)</td>
                <td class="matrix-cross">MONTHLY SUBSCRIPTIONS</td>
            </tr>
            <tr>
                <td>CENSORSHIP</td>
                <td class="matrix-check">NONE (UNFILTERED LOGIC)</td>
                <td class="matrix-cross">HEAVILY GUARDRAILED</td>
            </tr>
            <tr>
                <td>CONTROL</td>
                <td class="matrix-check">ROOT ACCESS / SSH</td>
                <td class="matrix-cross">CHAT INTERFACE ONLY</td>
            </tr>
        </tbody>
    </table>

    <div class="nav-footer">
        <a href="/research" class="nav-btn">VIEW_RESEARCH_PAPERS</a>
        <a href="/manual" class="nav-btn" style="border-color: var(--neon); color: var(--neon);">FULL_OPS_MANUAL</a>
    </div>

    <div class="brand-mark">
        <img src="/zero_og_image.png" width="300" height="300" alt="OpenZero Node Mark">
    </div>

    <footer>
        <p>PROJECT OPENZERO // ARCHITECT: SHAF BRADY // RESEARCHFORUMONLINE // 2026</p>
    </footer>
</div>

<script>
    // CYBERPUNK TYPING EFFECT
    const text = "curl -sL https://openzero.talktoai.org/install.sh | bash";
    const element = document.getElementById("typewriter");
    let index = 0;

    function typeWriter() {
        if (index < text.length) {
            element.innerHTML += text.charAt(index);
            index++;
            setTimeout(typeWriter, 50); // Typing speed
        } else {
            element.innerHTML += '<span class="cursor">_</span>';
        }
    }
    
    // Start typing when page loads
    window.onload = typeWriter;
</script>

</body>
</html>
